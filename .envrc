# Auto-activate venv (prefer ramdisk if available)
if [ -d /mnt/ramdisk/venvs/engram-mcp ]; then
    source /mnt/ramdisk/venvs/engram-mcp/bin/activate
else
    source /mnt/cache/venvs/engram-mcp/bin/activate
fi

export CHAINMIND_PATH=/mnt/dev/ai/chainmind
